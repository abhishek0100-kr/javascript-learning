//for
/*
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}
//just basic things that we know already 
*/

//break and continue 

for (let index = 1; index < 20; index++) {
    if(index==5){
        console.log(`detected 5`);
        continue
        
    }
    console.log(`value of i is ${index}`);
    
    
}